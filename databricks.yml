bundle:
  name: cartola_fc_pipeline

variables:
  catalog:
    default: cartola_fc
  schema_bronze:
    default: bronze
  schema_silver:
    default: silver
  schema_gold:
    default: gold
  schema_models:
    default: models

workspace:
  host: https://dbc-44d30137-2797.cloud.databricks.com
  profile: teste

targets:
  dev:
    mode: development

  prod:
    mode: production

include:
  - resources/cartola_pipeline.job.yml
